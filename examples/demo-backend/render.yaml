services:
  - type: web
    name: demo-backend
    env: go
    plan: free
    rootDir: examples/demo-backend
    buildCommand: "go mod download && go build -o server ./cmd/server"
    startCommand: "./server"

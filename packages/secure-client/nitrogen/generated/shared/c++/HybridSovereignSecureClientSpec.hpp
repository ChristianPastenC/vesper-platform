///
/// HybridSovereignSecureClientSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `QueueStatus` to properly resolve imports.
namespace margelo::nitro::secureclient { struct QueueStatus; }

#include <string>
#include <NitroModules/ArrayBuffer.hpp>
#include "QueueStatus.hpp"
#include <vector>
#include <NitroModules/Null.hpp>
#include <variant>

namespace margelo::nitro::secureclient {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `SovereignSecureClient`
   * Inherit this class to create instances of `HybridSovereignSecureClientSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSovereignSecureClient: public HybridSovereignSecureClientSpec {
   * public:
   *   HybridSovereignSecureClient(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSovereignSecureClientSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSovereignSecureClientSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSovereignSecureClientSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual bool executeTransaction(const std::string& id, const std::shared_ptr<ArrayBuffer>& serializedRequest, double ttl) = 0;
      virtual QueueStatus getQueueStatus() = 0;
      virtual void toggleNetworkSim(bool online) = 0;
      virtual void dequeueTransaction(const std::string& id) = 0;
      virtual bool verifyIntegrity() = 0;
      virtual void clearQueue() = 0;
      virtual void zeroize(const std::string& id) = 0;
      virtual std::vector<std::string> getQueueIds() = 0;
      virtual std::variant<nitro::NullType, std::shared_ptr<ArrayBuffer>> getTransactionPayload(const std::string& id) = 0;
      virtual std::string base64UrlEncode(const std::shared_ptr<ArrayBuffer>& data) = 0;
      virtual std::shared_ptr<ArrayBuffer> getTelemetrySnapshot() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "SovereignSecureClient";
  };

} // namespace margelo::nitro::secureclient
